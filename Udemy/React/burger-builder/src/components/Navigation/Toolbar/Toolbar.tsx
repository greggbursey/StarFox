import React from "react";
import Logo from "../../Logo/Logo";

const toolbar = (props: any): any => {
    <header>
        <div>MENU</div>
        <Logo></Logo>
        <nav>
            ...
        </nav>
    </header>
};

export default toolbar;
